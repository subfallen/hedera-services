# SPDX-License-Identifier: Apache-2.0
name: "ZXC: Execute HAPI Tests"
on:
  workflow_call:
    inputs:
      ref:
        description: "The branch, tag or SHA to checkout."
        required: false
        type: string
        default: ""
      java-distribution:
        description: "Java JDK Distribution:"
        type: string
        required: false
        default: "temurin"
      java-version:
        description: "Java JDK Version:"
        type: string
        required: false
        default: "25"
      node-version:
        description: "NodeJS Version:"
        type: string
        required: false
        default: "20"
      enable-network-log-capture:
        description: "Network Log Capture Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-misc:
        description: "HAPI Testing (misc) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-misc-records:
        description: "HAPI Testng (misc records) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-crypto:
        description: "HAPI Testing (crypto) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-simplefees:
        description: "HAPI Testing (simplefees) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-token:
        description: "HAPI Testing (token) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-smart-contract:
        description: "HAPI Testing (smart contract) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-time-consuming:
        description: "HAPI Testing (time consuming) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-restart:
        description: "HAPI Testing (restart) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-iss:
        description: "HAPI Testing (ISS) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-bn-communication:
        description: "HAPI Testing (Block Node Communication) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-nd-reconnect:
        description: "HAPI Testing (node death reconnect) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-atomic-batch:
        description: "HAPI Testing (atomic batch) Enabled"
        type: string
        required: false
        default: "false"
      enable-hapi-tests-state-throttling:
        description: "HAPI Testing (state throttling) Enabled"
        type: string
        required: false
        default: "false"
      mats-suffix:
        description: "MATS Tag Suffix designation"
        type: string
        required: false
        default: ""
    secrets:
      access-token:
        description: "The GitHub access token used to checkout the repository, submodules, and make GitHub API calls."
        required: true
      gradle-cache-username:
        description: "The username used to authenticate with the Gradle Build Cache Node."
        required: true
      gradle-cache-password:
        description: "The password used to authenticate with the Gradle Build Cache Node."
        required: true
    outputs:
      failure-mode:
        description: "Failure Mode"
        value: ${{ jobs.set-failure-mode.outputs.failure-mode }}

defaults:
  run:
    shell: bash

permissions:
  id-token: write
  actions: read
  pull-requests: write
  statuses: write
  checks: write
  contents: read

env:
  GRADLE_CACHE_USERNAME: ${{ secrets.gradle-cache-username }}
  GRADLE_CACHE_PASSWORD: ${{ secrets.gradle-cache-password }}
  GRADLE_EXEC: ionice -c 2 -n 2 nice -n 19 ./gradlew
  CG_EXEC: ionice -c 2 -n 2 nice -n 19

jobs:
  hapi-tests-misc:
    name: "HAPI Tests (Misc)"
    if: ${{ inputs.enable-hapi-tests-misc == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Misc)
        id: gradle-hapi-misc
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        # Run each task in isolation with clean builds because we dynamically reconfigure Log4j for each mode
        run: ${GRADLE_EXEC} hapiTestMisc --no-daemon && ${GRADLE_EXEC} hapiTestMiscEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestMiscRepeatable --no-daemon && ${GRADLE_EXEC} hapiTestMiscSerial --no-daemon

      - name: Publish HAPI Test (Misc) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Misc) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Misc) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Misc) Reports
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Misc) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-misc.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Misc) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Misc) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-misc.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Misc) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-misc-records:
    name: "HAPI Tests (Misc Records)"
    if: ${{ inputs.enable-hapi-tests-misc-records == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Misc Records)
        id: gradle-hapi-misc-records
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestMiscRecords && ${GRADLE_EXEC} hapiTestMiscRecordsSerial

      - name: Publish HAPI Test (Misc Records) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Misc Records) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Misc Records) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Misc Records) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Misc Records) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-misc-records.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Misc Records) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Misc Records) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-misc-records.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Misc Records) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-crypto:
    name: "HAPI Tests (Crypto)"
    if: ${{ inputs.enable-hapi-tests-crypto == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Crypto)
        id: gradle-hapi-crypto
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        # Crypto uses same task for PR and MATS (no MATS suffix)
        run: ${GRADLE_EXEC} hapiTestCrypto --no-daemon && ${GRADLE_EXEC} hapiTestCryptoEmbedded --no-daemon && ${GRADLE_EXEC} hapiTestCryptoSerial --no-daemon

      - name: Publish HAPI Test (Crypto) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Crypto) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Crypto) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Crypto) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Crypto) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-crypto.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Crypto) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Crypto) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-crypto.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Crypto) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-simple-fees:
    name: "HAPI Tests (Simple Fees)"
    if: ${{ inputs.enable-hapi-tests-simplefees == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Simple Fees)
        id: gradle-hapi-simplefees
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        # Simple Fees uses same task for PR and MATS (no MATS suffix)
        run: ${GRADLE_EXEC} hapiTestSimpleFees --no-daemon && ${GRADLE_EXEC} hapiEmbeddedSimpleFees --no-daemon && ${GRADLE_EXEC} hapiTestSimpleFeesSerial --no-daemon

      - name: Publish HAPI Test (Simple Fees) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Simple Fees) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Simple Fees) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Simple Fees) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Simple Fees) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-simplefees.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Simple Fees) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Simple Fees) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-simplefees.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Simple Fees) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-token:
    name: "HAPI Tests (Token)"
    if: ${{ inputs.enable-hapi-tests-token == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Token)
        id: gradle-hapi-token
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestToken --no-daemon && ${GRADLE_EXEC} hapiTestTokenSerial --no-daemon

      - name: Publish HAPI Test (Token) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Token) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/**/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Token) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Token) Report
          path: "**/test-clients/build/test-results/**/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Token) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-token.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Token) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Token) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-token.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Token) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-smart-contracts:
    name: "HAPI Tests (Smart Contracts)"
    if: ${{ inputs.enable-hapi-tests-smart-contract == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Smart Contract)
        id: gradle-hapi-smart-contract
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestSmartContract${{ inputs.mats-suffix || '' }}

      - name: Publish HAPI Test (Smart Contract) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Smart Contract) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Smart Contract) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Smart Contract) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Smart Contract) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-smart-contract.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Smart Contract) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Smart Contract) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-smart-contract.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Smart Contract) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-time-consuming:
    name: "HAPI Tests (Time Consuming)"
    if: ${{ inputs.enable-hapi-tests-time-consuming == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Time Consuming)
        id: gradle-hapi-time-consuming
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestTimeConsuming && ${GRADLE_EXEC} hapiTestTimeConsumingSerial

      - name: Publish HAPI Test (Time Consuming) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Time Consuming) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Time Consuming) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Time Consuming) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Time Consuming) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-time-consuming.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Time Consuming) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Time Consuming) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-time-consuming.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Time Consuming) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-restart:
    name: "HAPI Tests (Restart)"
    if: ${{ inputs.enable-hapi-tests-restart == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Restart)
        id: gradle-hapi-restart
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestRestart

      - name: Publish HAPI Test (Restart) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Restart) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Restart) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Restart) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Restart) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-restart.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Restart) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Restart) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-restart.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Restart) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-nd-reconnect:
    name: "HAPI Tests (ND Reconnect)"
    if: ${{ inputs.enable-hapi-tests-nd-reconnect == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Node Death Reconnect)
        id: gradle-hapi-nd-reconnect
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestNDReconnect

      - name: Publish HAPI Test (Node Death Reconnect) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Node Death Reconnect) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Node Death Reconnect) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Node Death Reconnect) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Node Death Reconnect) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-nd-reconnect.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Node Death Reconnect) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Node Death Reconnect) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-nd-reconnect.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Node Death Reconnect) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-iss:
    name: "HAPI Tests (ISS)"
    if: ${{ inputs.enable-hapi-tests-iss == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (ISS)
        id: gradle-hapi-iss
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestIss

      - name: Publish HAPI Test (ISS) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (ISS) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (ISS) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (ISS) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (ISS) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-iss.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (ISS) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (ISS) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-iss.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (ISS) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7`

  hapi-tests-bn-comms:
    name: "HAPI Tests (BN Comms)"
    if: ${{ inputs.enable-hapi-tests-bn-communication == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Block Node Communication)
        id: gradle-hapi-bn-communication
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestBlockNodeCommunication

      - name: Publish HAPI Test (Block Node Communication) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Block Node Communication) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Block Node Communication) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Block Node Communication) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Block Node Communication) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-bn-communication.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Block Node Communication) Network Logs
          path: |
            hedera-node/test-clients/build/*-test/**/output/**
          retention-days: 7

      - name: Upload HAPI Test (Block Node Communication) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-bn-communication.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Block Node Communication) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-atomic-batch:
    name: "HAPI Tests (Atomic Batch)"
    if: ${{ inputs.enable-hapi-tests-atomic-batch == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (Atomic Batch)
        id: gradle-hapi-atomic-batch
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestAtomicBatch${{ inputs.mats-suffix || '' }}

      - name: Publish HAPI Test (Atomic Batch) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (Atomic Batch) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (Atomic Batch) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (Atomic Batch) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (Atomic Batch) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-atomic-batch.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Atomic Batch) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (Atomic Batch) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-atomic-batch.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (Atomic Batch) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  hapi-tests-state-throttling:
    name: "HAPI Tests (State Throttling)"
    if: ${{ inputs.enable-hapi-tests-state-throttling == 'true' }}
    runs-on: hl-cn-hapi-lin-lg
    steps:
      - name: Prepare Runner
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1
        with:
          checkout: "true"
          checkout-ref: "${{ inputs.ref }}"
          checkout-fetch-depth: "1"
          checkout-token: "${{ secrets.access-token }}"
          setup-java: "true"
          java-version: "${{ inputs.java-version }}"
          java-distribution: "${{ inputs.java-distribution }}"
          setup-gradle: "true"
          gradle-cache-read-only: "false"
          setup-node: "true"
          node-version: "${{ inputs.node-version }}"

      - name: HAPI Testing (State Throttling)
        id: gradle-hapi-state-throttling
        env:
          LC_ALL: en.UTF-8
          LANG: en_US.UTF-8
        run: ${GRADLE_EXEC} hapiTestStateThrottling

      - name: Publish HAPI Test (State Throttling) Report
        uses: step-security/publish-unit-test-result-action@7dff603bf17ef13dee847147bef8d7cd1728b566 # v2.22.0
        with:
          check_name: "Node: HAPI Test (State Throttling) Results"
          json_thousands_separator: ","
          junit_files: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          comment_mode: errors # only comment if we could not find or parse the JUnit XML files

      - name: Upload HAPI Test (State Throttling) Report Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: HAPI Test (State Throttling) Report
          path: "**/test-clients/build/test-results/testSubprocess/TEST-*.xml"
          retention-days: 7

      - name: Upload HAPI Test (State Throttling) Network Logs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ inputs.enable-network-log-capture && steps.gradle-hapi-state-throttling.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (State Throttling) Network Logs
          path: |
            hedera-node/test-clients/build/hapi-test/**/output/**
            hedera-node/test-clients/build/hapi-test/*.log
            hedera-node/test-clients/output/**
          retention-days: 7

      - name: Upload HAPI Test (State Throttling) Failure Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: ${{ steps.gradle-hapi-state-throttling.conclusion == 'failure' && !cancelled() }}
        with:
          name: HAPI Test (State Throttling) Failure Artifacts
          path: |
            hedera-node/test-clients/build/**/*.blk.gz
            hedera-node/test-clients/build/**/*.rcd.gz
            hedera-node/test-clients/build/**/*.pb
            hedera-node/test-clients/build/**/*.pces
          retention-days: 7

  set-failure-mode:
    name: "Set Failure Mode"
    runs-on: hl-cn-default-lin-sm
    needs:
      - hapi-tests-misc
      - hapi-tests-misc-records
      - hapi-tests-crypto
      - hapi-tests-simple-fees
      - hapi-tests-token
      - hapi-tests-smart-contracts
      - hapi-tests-time-consuming
      - hapi-tests-restart
      - hapi-tests-nd-reconnect
      - hapi-tests-iss
      - hapi-tests-bn-comms
      - hapi-tests-atomic-batch
      - hapi-tests-state-throttling
    if: ${{ !cancelled() && always() }} # always run unless cancelled
    outputs:
      failure-mode: ${{ steps.set-failure-mode.outputs.failure-mode }}
    steps:
      - name: Initialize GitHub Job
        uses: pandaswhocode/initialize-github-job@bfe0b1633012cee3033c757c7095d49c360e23a6 # v1.0.1

      - name: Set Failure Mode
        id: set-failure-mode
        run: |
          job_statuses=(
            "${{ needs.hapi-tests-misc.result || 'skipped' }}"
            "${{ needs.hapi-tests-misc-records.result || 'skipped' }}"
            "${{ needs.hapi-tests-crypto.result || 'skipped' }}"
            "${{ needs.hapi-tests-simple-fees.result || 'skipped' }}"
            "${{ needs.hapi-tests-token.result || 'skipped' }}"
            "${{ needs.hapi-tests-smart-contracts.result || 'skipped' }}"
            "${{ needs.hapi-tests-time-consuming.result || 'skipped' }}"
            "${{ needs.hapi-tests-restart.result || 'skipped' }}"
            "${{ needs.hapi-tests-nd-reconnect.result || 'skipped' }}"
            "${{ needs.hapi-tests-iss.result || 'skipped' }}"
            "${{ needs.hapi-tests-bn-comms.result || 'skipped' }}"
            "${{ needs.hapi-tests-atomic-batch.result || 'skipped' }}"
            "${{ needs.hapi-tests-state-throttling.result || 'skipped' }}"
          )

          # final outputs
          failure_mode="none"

          has_failure() {
            local arr=("$@")
            for status in "${arr[@]}"; do
              [[ "${status}" == "failure" ]] && return 0
            done
            return 1
          }

          if has_failure "${job_statuses[@]}"; then
            failure_mode="general"
          fi

          echo "failure-mode=${failure_mode}" >> "${GITHUB_OUTPUT}"
